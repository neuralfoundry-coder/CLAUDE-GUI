apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: claudegui-dev

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/part-of: claudegui
  app.kubernetes.io/env: local

images:
  - name: claudegui:dev
    newTag: dev
